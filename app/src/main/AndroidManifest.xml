<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nie.nieapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>

    <application
        android:name=".dagger.base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".glide.GlideActivity" />
        <activity android:name=".dagger.module.Dagger2Activity"></activity>
        <activity android:name=".dagger.inject.InjectActivity"></activity>
        <activity android:name=".dagger.case1.Case1Activity"></activity>

        <activity android:name=".dagger.normal.NormalActivity">

        </activity>


        <activity android:name=".dagger.android.ADActivity">

        </activity>

        <activity android:name=".dagger.android.TestActivity" />

        <activity android:name=".dagger.optandroid.OptActivity">

        </activity>

        <activity android:name=".dagger.optandroid.Opt2Activity">

        </activity>
    </application>

</manifest>